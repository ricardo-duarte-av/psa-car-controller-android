# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
build/
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_
*.dex
/app/release

# Compiled class file
*.class

# Log file
*.log

# Package Files
*.jar
*.war
*.zip
*.tar.gz

# Keep the Gradle wrapper jar despite the *.jar rule above
!gradle/wrapper/gradle-wrapper.jar

# Signing material — never commit keystores or credentials
/signing/
*.jks
*.keystore
keystore.properties

# virtual machine crash logs
hs_err_pid*
replay_pid*

# Kotlin
.kotlin/

# Test-server credentials (live car — keep local only)
test-server.properties

# Project instructions (holds test-server credentials — keep local only)
CLAUDE.md
